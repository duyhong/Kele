# Kele

This is a Ruby Gem API client to access the Bloc API.

This project allows users to integrate Kele with other software.

Users can initialize and authorize Kele with a username and password.

The API converts the JSON response to Ruby.

Users can update resources via the API.
